import {ADD_JOB} from "../actionTypes/jobTypes";

const initialState = {
  allJobs: []
}

export const jobReducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
